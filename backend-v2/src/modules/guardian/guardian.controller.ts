import { Body,Controller,Get,Param,Post } from "@nestjs/common";import { IsIn,IsOptional,IsString,MaxLength } from "class-validator";import { CurrentUser } from "../../common/decorators/current-user.decorator";import { RequireCapabilities } from "../../common/decorators/capabilities.decorator";import { ok } from "../../common/utils/envelope";import { AuthenticatedUser } from "../auth/auth.service";import { GuardianService } from "./guardian.service";
class EncouragementDto{@IsString()@MaxLength(500)message!:string;@IsOptional()@IsIn(["SUPPORT","PROUD","KEEP_GOING","CELEBRATE"])kind?:string;}
@Controller("guardian")export class GuardianController{constructor(private guardian:GuardianService){}@Get("students")@RequireCapabilities("guardian.students.read")students(@CurrentUser()u:AuthenticatedUser){return this.guardian.students(u.id).then(ok);}@Get("students/:id/dashboard")@RequireCapabilities("guardian.dashboard.read")dashboard(@CurrentUser()u:AuthenticatedUser,@Param("id")id:string){return this.guardian.dashboard(u.id,id).then(ok);}@Get("students/:id/progress")@RequireCapabilities("guardian.progress.read")progress(@CurrentUser()u:AuthenticatedUser,@Param("id")id:string){return this.guardian.progress(u.id,id).then(ok);}@Get("students/:id/schedule")@RequireCapabilities("guardian.schedule.read")schedule(@CurrentUser()u:AuthenticatedUser,@Param("id")id:string){return this.guardian.schedule(u.id,id).then(ok);}@Get("students/:id/exams")@RequireCapabilities("guardian.exams.read")exams(@CurrentUser()u:AuthenticatedUser,@Param("id")id:string){return this.guardian.exams(u.id,id).then(ok);}@Get("students/:id/reports")@RequireCapabilities("guardian.reports.read")reports(@CurrentUser()u:AuthenticatedUser,@Param("id")id:string){return this.guardian.reports(u.id,id).then(ok);}@Post("students/:id/encouragement")@RequireCapabilities("guardian.encouragement.create")encourage(@CurrentUser()u:AuthenticatedUser,@Param("id")id:string,@Body()body:EncouragementDto){return this.guardian.encourage(u.id,id,body).then(ok);}}
