# Claude Code repository instructions

Use `AGENTS.md` as the canonical repository-wide engineering contract.

Before broad file search, query Graphify when the repository graph exists. Install the project integration with `graphify claude install --project` (optionally `--strict`) and build/update with `/graphify .` or `/graphify . --update`.

Do not treat Graphify output as authoritative over current source; verify files before edits.
