# Backend on Runflare

Use this directory as the Docker build context. Expose container port 4000, configure `/health`, and mount a persistent disk at `/data`. Copy the keys from `runflare.env.example` into Runflare and replace every placeholder secret.

See `../RUNFLARE-DEPLOY.md` for the complete three-service order and verification checklist.

