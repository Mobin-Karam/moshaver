# Admin View Modules

Extract admin screens one domain at a time while keeping the static vanilla-JavaScript architecture.

Extracted in v1.4.3:

- `reports.js` — reports list
- `students.js` — active-student profile editing and chat handoff
- `subjects.js` — student subject settings and subject creation
- `system.js` — sessions, releases, import history, and password change UI

Next safe extraction targets are chat, planner, exams/quizzes, imports, dashboard, and live activity. Preserve API paths and behavior during each move.
