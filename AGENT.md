# Agent entrypoint

Read `AGENTS.md` before modifying this repository.

Discovery policy: use Graphify first when `graphify-out/graph.json` exists, then verify relevant source files before editing. See `docs/architecture/graphify.md`.
