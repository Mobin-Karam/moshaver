import { MigrationInterface,QueryRunner } from "typeorm";export class SystemOperations1724141900000 implements MigrationInterface{name="SystemOperations1724141900000";async up(q:QueryRunner){await q.query(`CREATE TABLE app_versions(app varchar(40) PRIMARY KEY NOT NULL,version varchar(40) NOT NULL,notes varchar NOT NULL DEFAULT (''),updatedAt datetime NOT NULL DEFAULT (datetime('now')))`);await q.query(`CREATE TABLE app_releases(id varchar PRIMARY KEY NOT NULL,app varchar(40) NOT NULL,version varchar(40) NOT NULL,notes varchar NOT NULL DEFAULT (''),createdAt datetime NOT NULL DEFAULT (datetime('now')))`);await q.query(`CREATE INDEX IDX_release_app_created ON app_releases(app,createdAt)`);for(const app of ["admin","student-web","student-android","student-desktop"])await q.query(`INSERT OR IGNORE INTO app_versions(app,version,notes)VALUES(?,'2.0.0','')`,[app]);for(const key of ["release.read","release.manage","database.read","database.backup","database.restore"])await q.query(`INSERT OR IGNORE INTO permissions(id,code,description)VALUES(lower(hex(randomblob(16))),?,'')`,[key]);await q.query(`INSERT OR IGNORE INTO role_permissions(id,roleId,permissionId)SELECT lower(hex(randomblob(16))),r.id,p.id FROM roles r CROSS JOIN permissions p WHERE r.code='PLATFORM_ADMIN'`);}async down(q:QueryRunner){await q.query(`DROP TABLE app_releases`);await q.query(`DROP TABLE app_versions`);}}
