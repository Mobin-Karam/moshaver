import { MigrationInterface,QueryRunner } from "typeorm";export class GuardianWorkflow1724141500000 implements MigrationInterface{name="GuardianWorkflow1724141500000";async up(q:QueryRunner){await q.query(`CREATE TABLE encouragements(id varchar PRIMARY KEY NOT NULL,message varchar(500) NOT NULL,kind varchar(40) NOT NULL DEFAULT ('SUPPORT'),createdAt datetime NOT NULL DEFAULT (datetime('now')),studentId varchar NOT NULL,guardianId varchar NOT NULL,CONSTRAINT FK_encouragement_student FOREIGN KEY(studentId) REFERENCES students(id) ON DELETE CASCADE,CONSTRAINT FK_encouragement_guardian FOREIGN KEY(guardianId) REFERENCES users(id) ON DELETE CASCADE)`);await q.query(`CREATE INDEX IDX_encouragement_student ON encouragements(studentId,createdAt)`);for(const key of ["guardian.students.read","guardian.dashboard.read","guardian.progress.read","guardian.schedule.read","guardian.exams.read","guardian.reports.read","guardian.encouragement.create"])await q.query(`INSERT OR IGNORE INTO permissions(id,code,description)VALUES(lower(hex(randomblob(16))),?,'')`,[key]);await q.query(`INSERT OR IGNORE INTO role_permissions(id,roleId,permissionId)SELECT lower(hex(randomblob(16))),r.id,p.id FROM roles r CROSS JOIN permissions p WHERE r.code IN ('GUARDIAN','PLATFORM_ADMIN') AND (r.code='PLATFORM_ADMIN' OR p.code LIKE 'guardian.%')`);}async down(q:QueryRunner){await q.query(`DROP TABLE encouragements`);}}
