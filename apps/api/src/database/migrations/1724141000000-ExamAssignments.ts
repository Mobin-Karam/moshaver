import{MigrationInterface,QueryRunner}from"typeorm";export class ExamAssignments1724141000000 implements MigrationInterface{name="ExamAssignments1724141000000";async up(q:QueryRunner){for(const sql of[`ALTER TABLE exams ADD COLUMN organizationId varchar REFERENCES organizations(id) ON DELETE CASCADE`,`ALTER TABLE exams ADD COLUMN createdById varchar REFERENCES users(id) ON DELETE SET NULL`]){try{await q.query(sql)}catch(error){if(!String(error).includes("duplicate column"))throw error}}await q.query(`CREATE INDEX IF NOT EXISTS IDX_exams_organization ON exams(organizationId)`);await q.query(`CREATE TABLE IF NOT EXISTS exam_assignments(id varchar PRIMARY KEY NOT NULL, examId varchar NOT NULL, studentId varchar NOT NULL, assignedById varchar NOT NULL, assignedAt datetime NOT NULL DEFAULT CURRENT_TIMESTAMP, createdAt datetime NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT FK_exam_assignment_exam FOREIGN KEY(examId) REFERENCES exams(id) ON DELETE CASCADE, CONSTRAINT FK_exam_assignment_student FOREIGN KEY(studentId) REFERENCES students(id) ON DELETE CASCADE, CONSTRAINT FK_exam_assignment_actor FOREIGN KEY(assignedById) REFERENCES users(id) ON DELETE RESTRICT)`);await q.query(`CREATE UNIQUE INDEX IF NOT EXISTS IDX_exam_assignment_pair ON exam_assignments(examId,studentId)`);await q.query(`CREATE INDEX IF NOT EXISTS IDX_exam_assignment_student ON exam_assignments(studentId,assignedAt)`);await q.query(`INSERT OR IGNORE INTO role_permissions(id,roleId,permissionId) SELECT lower(hex(randomblob(16))),r.id,p.id FROM roles r CROSS JOIN permissions p WHERE r.code='PLATFORM_ADMIN'`)}async down(q:QueryRunner){await q.query(`DROP TABLE IF EXISTS exam_assignments`)}}
