import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { AlertTriangle, History } from "lucide-react";
import { getSelectedApiVersion } from "../../../shared/api/api";
import { useAuth } from "../../auth/hooks/useAuth";
import { useModal } from "../../../shared/ui/modal";
import { notify } from "../../../shared/ui/notifications";
import { Button, Card } from "../../../shared/ui/ui";
import { changeAdminPassword, downloadDatabaseBackup, getAudit, getDatabaseMeta, getImportHistory, getReleases, getSessions, restoreDatabase, saveAppRelease } from "../api/system.api";
import { AccountSecurityPanel } from "../components/AccountSecurityPanel";
import { DatabaseBackupPanel } from "../components/DatabaseBackupPanel";
import { ReleasePanel } from "../components/ReleasePanel";
import { SystemHistory } from "../components/SystemHistory";
import { SystemMetric } from "../components/SystemMetric";
import { SystemSessionsPanel } from "../components/SystemSessionsPanel";
export function SystemPage() {
 const qc = useQueryClient(); const modal = useModal(); const auth = useAuth();
 const systemAvailable=auth.can("database.read")||auth.can("release.read")||auth.can("audit.read");
 const [file,setFile] = useState<File|null>(null); const [passwords,setPasswords] = useState({currentPassword:"",newPassword:"",confirmPassword:""}); const [release,setRelease] = useState({app:"admin",version:"",notes:""}); const[historyTab,setHistoryTab]=useState<"audit"|"imports"|"releases">("audit");
 const database = useQuery({queryKey:["system-database"],queryFn:getDatabaseMeta,enabled:systemAvailable}); const sessions=useQuery({queryKey:["sessions"],queryFn:getSessions}); const imports=useQuery({queryKey:["import-history"],queryFn:getImportHistory,enabled:systemAvailable&&historyTab==="imports"}); const releases=useQuery({queryKey:["app-releases"],queryFn:getReleases,enabled:systemAvailable&&historyTab==="releases"}); const audit=useQuery({queryKey:["audit"],queryFn:getAudit,enabled:systemAvailable&&historyTab==="audit"});
 const restore=useMutation({mutationFn:()=>{if(!file) throw new Error("فایل انتخاب نشده است."); return restoreDatabase(file);},onSuccess:()=>{setFile(null);notify("نسخه پشتیبان اعتبارسنجی و برای بازیابی ثبت شد.");void qc.invalidateQueries({queryKey:["system-database"]})},onError:(error)=>notify(error instanceof Error?error.message:"بازیابی پایگاه داده انجام نشد.","error")});
 const backup=useMutation({mutationFn:downloadDatabaseBackup,onError:(error)=>notify(error instanceof Error?error.message:"دریافت نسخه پشتیبان انجام نشد.","error")});
 const changePassword=useMutation({mutationFn:()=>changeAdminPassword(passwords),onSuccess:()=>{setPasswords({currentPassword:"",newPassword:"",confirmPassword:""});notify("رمز مدیر تغییر کرد و نشست‌های دیگر بسته شدند.")},onError:(error)=>notify(error instanceof Error?error.message:"تغییر رمز انجام نشد.","error")});
 const saveRelease=useMutation({mutationFn:()=>saveAppRelease(release),onSuccess:()=>{setRelease({...release,version:"",notes:""});notify("انتشار جدید ثبت شد.");void qc.invalidateQueries({queryKey:["app-releases"]})},onError:(error)=>notify(error instanceof Error?error.message:"ثبت انتشار انجام نشد.","error")});
 async function download(){const result=await backup.mutateAsync(); const url=URL.createObjectURL(result.blob); const anchor=document.createElement("a"); anchor.href=url; anchor.download=result.filename; anchor.click(); URL.revokeObjectURL(url);notify("نسخه پشتیبان دانلود شد.");}
 const meta=database.data;
 return <div className="grid gap-4">{!systemAvailable?<div className="flex items-start gap-2 rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900" role="status"><AlertTriangle className="mt-0.5 shrink-0" size={17}/><span>API v2 هنوز قراردادهای پایگاه داده، انتشار و ممیزی را ارائه نمی‌کند؛ این ابزارهای عملیاتی تا تکمیل هم‌ارزی غیرفعال‌اند. امنیت حساب و نشست‌ها قابل استفاده هستند.</span></div>:null}<section className="grid grid-cols-2 gap-2 lg:grid-cols-4"><SystemMetric label="پایگاه داده" value={systemAvailable?(meta?.database||meta?.status||(database.isLoading?"در حال بررسی…":"نامشخص")):"در انتظار v2"}/><SystemMetric label="نسخه" value={meta?.version||`API ${getSelectedApiVersion()}`}/><SystemMetric label="نشست فعال" value={String(meta?.activeSessions??sessions.data?.length??0)}/><SystemMetric label="اتصال زنده" value={String(meta?.realtimeConnections||0)}/></section><section className="grid gap-4 lg:grid-cols-2"><AccountSecurityPanel passwords={passwords} setPasswords={setPasswords} busy={changePassword.isPending} onSubmit={()=>void modal.confirm({title:"تغییر رمز مدیر؟",description:"هویت شما دوباره بررسی و نشست‌های دیگر این حساب بسته می‌شوند.",confirmLabel:"تغییر رمز"}).then((confirmed)=>confirmed&&changePassword.mutate())}/><SystemSessionsPanel sessions={sessions.data} loading={sessions.isLoading}/></section>{systemAvailable?<><DatabaseBackupPanel file={file} busy={restore.isPending} downloading={backup.isPending} setFile={setFile} onDownload={()=>void download()} onRestore={()=>void modal.confirm({title:"آخرین تأیید بازیابی پایگاه داده",description:`فایل ${file?.name||"انتخاب‌شده"} پس از اعتبارسنجی جایگزین داده فعلی می‌شود. پیش از اجرا یک snapshot خودکار ساخته خواهد شد.`,tone:"danger",confirmLabel:"بازیابی پایگاه داده"}).then((confirmed)=>confirmed&&restore.mutate())}/><ReleasePanel release={release} setRelease={setRelease} busy={saveRelease.isPending} onSubmit={()=>void modal.confirm({title:"ثبت انتشار جدید؟",description:`${release.app} • ${release.version}`,confirmLabel:"ثبت انتشار"}).then((confirmed)=>confirmed&&saveRelease.mutate())}/><Card className="p-2"><div className="flex flex-wrap items-center gap-1" role="tablist" aria-label="نوع تاریخچه"><History size={16} className="mx-2 text-slate-400"/>{([['audit','ممیزی'],['imports','ورود JSON'],['releases','انتشارها']] as const).map(([id,label])=><Button key={id} className="h-8" variant={historyTab===id?"primary":"ghost"} onClick={()=>setHistoryTab(id)}>{label}</Button>)}</div></Card>{historyTab==="audit"?<SystemHistory title="گزارش ممیزی" rows={audit.data} loading={audit.isLoading} error={audit.isError} onRetry={()=>void audit.refetch()}/>:historyTab==="imports"?<SystemHistory title="تاریخچه ورود JSON" rows={imports.data} loading={imports.isLoading} error={imports.isError} onRetry={()=>void imports.refetch()}/>:<SystemHistory title="تاریخچه انتشار" rows={releases.data} loading={releases.isLoading} error={releases.isError} onRetry={()=>void releases.refetch()}/>}</>:null}</div>;
}
