import { Database, Download, Upload } from "lucide-react";
import { Button, Card, Field, Input } from "../../../shared/ui/ui";
export function DatabaseBackupPanel({ file, busy, setFile, onDownload, onRestore }: { file: File | null; busy: boolean; setFile: (file: File | null) => void; onDownload: () => void; onRestore: () => void }) { return <Card><div className="mb-4 flex items-center gap-2"><Database size={18}/><h3 className="font-bold">پشتیبان SQLite</h3></div><div className="flex flex-wrap items-end gap-3"><Button onClick={onDownload}><Download size={16}/>دانلود پشتیبان</Button><Field label="فایل بازیابی"><Input type="file" accept=".sqlite,.db,application/vnd.sqlite3" onChange={(e) => setFile(e.target.files?.[0] || null)} /></Field><Button loading={busy} variant="danger" disabled={!file || busy} onClick={onRestore}><Upload size={16}/>بازیابی</Button></div></Card>; }
