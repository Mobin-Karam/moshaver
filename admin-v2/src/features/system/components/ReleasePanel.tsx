import { Button, Card, Field, Input, Textarea } from "../../../shared/ui/ui";
import type { ReleaseDraft } from "../model/system.types";
export function ReleasePanel({ release, setRelease, busy, onSubmit }: { release: ReleaseDraft; setRelease: (value: ReleaseDraft) => void; busy: boolean; onSubmit: () => void }) { return <Card><h3 className="mb-3 font-bold">انتشار نسخه</h3><div className="grid gap-3 md:grid-cols-[160px_180px_1fr_auto]"><Field label="برنامه"><Input dir="ltr" value={release.app} onChange={(e) => setRelease({ ...release, app: e.target.value })}/></Field><Field label="نسخه"><Input dir="ltr" value={release.version} onChange={(e) => setRelease({ ...release, version: e.target.value })}/></Field><Field label="یادداشت انتشار"><Textarea rows={1} value={release.notes} onChange={(e) => setRelease({ ...release, notes: e.target.value })}/></Field><Button loading={busy} className="md:mt-6" disabled={!release.app || !release.version || busy} onClick={onSubmit}>ثبت انتشار</Button></div></Card>; }
