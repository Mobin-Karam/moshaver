# Gemini CLI repository instructions

Use `AGENTS.md` as the canonical repository-wide engineering contract.

Use Graphify as the first architecture/navigation layer when `graphify-out/graph.json` exists. Project setup can be installed with `graphify gemini install --project`; build or refresh through the Graphify skill, then verify source before editing.
